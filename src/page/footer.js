import React from 'react';

const Footer = () => {
    const styles = {
        backgroundColor: 'teal',
        padding: '30px 0'
    };
    return (
        <div style={styles}>
        </div>
    );
};

export default Footer;
